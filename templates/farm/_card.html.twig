{% extends 'common/_card.html.twig' %}

{% block content %}
    <h2 class="text-primary">{{ 'about'|trans }}</h2>
    <p class="lead mt-4">
        {{ 'farm_text'|trans|nl2br }}
    </p>
{% endblock %}